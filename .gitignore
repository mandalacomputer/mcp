node_modules/
dist/
*.log
.DS_Store
.env
.env.*
coverage/

# Reports written by the /grok-bug-hunt skill.
.grok-bug-hunt/

# Local issue-tracker wiring. Names a workspace, and no build reads it.
.claude/linear-sync.json

# Adversarial-review transcripts from the /fix-lanes workflow. They quote
# whatever the reviewer read, which in this repo means the platform, and
# check:internals does not scan this path.
.fix-lanes/
